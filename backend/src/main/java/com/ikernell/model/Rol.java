package com.ikernell.model;

/**
 * Enum que define los roles de usuario para el sistema IKernell.
 * Compatible con la gestión de seguridad de Spring Security (RBAC).
 */
public enum Rol {
    COORDINADOR,
    LIDER,
    DESARROLLADOR
}
